{{#if vehicle.isReady}}
  <button class="btn btn-primary btn-block btn-lg" {{action 'click'}}>Send Vehicle to Avis Fleet</button>
{{else}}
  {{#link-to 'checklist' vehicle class='btn btn-lg btn-block btn-warning'}}Not Ready{{/link-to}}
{{/if}}
